<?php
    // $titre = "Liste des catégories";
    // require_once 'partials/_header.php';

    // not_connected();
    
    // if($_SESSION['rolle'] === "eleve") redirect_to('profil.php?id=' . $_SESSION['id']);

    // if(!isset($_SESSION['auth'])) {
    //     redirect_to('login.php');
    // }

    // $query = "SELECT * FROM category ";
    // $params = [];

    //     //Gestion des paramètre de la recherche
    // if (!empty($_GET['q'])) {
    //     $query .= " WHERE title LIKE :q";
    //     // $queryCount .= " WHERE title LIKE :q OR author LIKE :q";
    //     $params['q'] = "%{$_GET['q']}%";
    // }

    // $q = $db->prepare($query);
    // $q->execute($params);
    // $categories = $q->fetchAll(PDO::FETCH_OBJ);
    

    // require_once 'views/_category_list.php';

    // require_once 'partials/_footer.php';